[project]
name = "flatris-infra"
version = "0.1.0"
description = "Flatris game deployment infrastructure on AWS"
requires-python = ">=3.9"
dependencies = [
    "pulumi>=3.0.0",
    "pulumi-aws>=6.0.0",
]